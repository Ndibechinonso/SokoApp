import React from 'react'
import AddButton from '../AddButton'
import './Addresses.css'


const Addresses = (props) => {
    return (
        <div>
            <AddButton text='Add New Address' />
        </div>
    )
}

export default Addresses